import { initSection, sections } from './components/sections'

import { initComments, comments } from './components/recent-comments'

import { related, initRelated } from './components/related'

import { entry, initEntry } from './components/entry'

export default {
  sections,
  comments,
  related,
  entry,

  initComments,
  initRelated,
  initEntry,
  initSection
}
